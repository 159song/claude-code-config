---
phase: quick
plan: 260413-k6w
type: execute
wave: 1
depends_on: []
files_modified:
  - ARCHITECTURE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "ARCHITECTURE.md contains a visual execution flow diagram showing the complete system lifecycle"
    - "The diagram shows all 4 entry paths: new-project, quick, phase lifecycle, autonomous"
    - "The diagram shows decision points, gates, and where parallelization occurs"
  artifacts:
    - path: "ARCHITECTURE.md"
      provides: "Overall execution flow diagram section"
      contains: "整体执行流程"
  key_links:
    - from: "execution flow diagram"
      to: "existing layer descriptions"
      via: "inserted as new section between 概述 and 系统分层"
---

<objective>
Add an overall execution flow diagram to ARCHITECTURE.md that visually represents how the entire WF system flows from user input to task completion.

Purpose: The current ARCHITECTURE.md has detailed layer descriptions, data flows, and design patterns, but lacks a high-level visual showing the complete execution lifecycle. This makes it hard for readers to quickly grasp how the pieces fit together before diving into individual layers.

Output: A new "整体执行流程" section in ARCHITECTURE.md with a Mermaid flowchart diagram.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@ARCHITECTURE.md
@wf/workflows/autonomous.md
@wf/workflows/execute-phase.md
@wf/workflows/new-project.md
@wf/workflows/quick.md
@wf/workflows/do.md

<interfaces>
The diagram must reflect these actual system flows extracted from the workflow files:

1. Entry routing (do.md):
   - Natural language input -> intent parsing -> route to one of: new-project, discuss-phase, plan-phase, execute-phase, verify-work, autonomous, quick, progress, next

2. New project flow (new-project.md):
   - gather_context -> configure -> research (4 parallel agents, optional) -> requirements -> roadmap (wf-roadmapper agent) -> initialize_state

3. Phase lifecycle (discuss-phase -> plan-phase -> execute-phase -> verify-work):
   - discuss: collect context -> CONTEXT.md
   - plan: optional research -> generate PLANs (wf-planner agent) -> quality gate (max 3 retries)
   - execute: load plans -> wave execution (parallel wf-executor agents per wave) -> wave merge -> post-wave check -> verify phase (wf-verifier agent)
   - verify: 4-level progressive verification (EXISTS -> SUBSTANTIVE -> WIRED -> DATA-FLOWING) -> gap closure if failures

4. Autonomous flow (autonomous.md):
   - parse flags -> phase loop { context check -> Skill(discuss) -> Skill(plan) -> Skill(execute) -> Skill(verify) -> gap closure if needed -> advance } -> completion

5. Quick task flow (quick.md):
   - understand task -> optional research -> optional discuss -> plan+execute (wf-executor agent) -> optional verify

6. Parallelization points:
   - new-project: 4 researcher agents in parallel
   - execute-phase: multiple wf-executor agents per wave (worktree isolation)
   - Within autonomous: each phase is sequential, but within each phase's execute step, waves are parallel

7. Key decision/gate points:
   - Context budget check (<40% remaining -> pause)
   - Plan quality gate (max 3 revision cycles)
   - Verification gate (fail -> gap closure -> re-verify once)
   - Agent completion marker routing (complete/partial/failed)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design and insert execution flow diagram into ARCHITECTURE.md</name>
  <files>ARCHITECTURE.md</files>
  <action>
Insert a new section "## 整体执行流程" between the existing "## 概述" section (ends at line 12) and "## 系统分层" section (starts at line 14).

The section should contain:

1. A brief intro paragraph (2-3 sentences) explaining the diagram shows the high-level execution paths through the WF system.

2. A Mermaid flowchart diagram (`mermaid` code block) that shows:

   **Entry layer (top):**
   - User input enters through `/wf-do` (natural language router) OR direct `/wf-*` commands
   - Routes to one of 4 main paths: new-project, quick, phase lifecycle, autonomous

   **Path 1 - New Project (`/wf-new-project`):**
   - Gather context -> Configure -> [Optional: 4x parallel researcher agents] -> Generate requirements (wf-roadmapper agent) -> Initialize STATE.md -> Done (suggest `/wf-discuss-phase 1` or `/wf-autonomous`)

   **Path 2 - Quick Task (`/wf-quick`):**
   - Understand task -> [Optional: research/discuss] -> Plan + Execute (wf-executor agent) -> [Optional: verify] -> Done

   **Path 3 - Phase Lifecycle (the core loop: discuss -> plan -> execute -> verify):**
   - `/wf-discuss-phase N` -> CONTEXT.md
   - `/wf-plan-phase N` -> [Optional: research] -> PLAN.md(s) -> Quality gate (max 3 retries)
   - `/wf-execute-phase N` -> Wave loop { parallel wf-executor agents } -> Wave merge -> Post-wave check -> wf-verifier agent -> 4-level verification
   - Verification result: PASS -> advance to next phase; FAIL -> gap closure (1 retry) -> re-verify -> still FAIL -> pause for user

   **Path 4 - Autonomous (`/wf-autonomous`):**
   - Shows it as a loop wrapping the Phase Lifecycle path: for each phase { context budget check -> discuss -> plan -> execute -> verify -> advance }

   **Cross-cutting elements to show:**
   - Parallelization markers: "4x parallel" on researcher agents, "parallel per wave" on executor agents
   - Gate/decision diamonds: context budget check, plan quality gate, verification gate, agent completion status routing
   - State updates: all state changes go through wf-tools CLI (shown as a side note)
   - Hook monitoring: context-monitor and prompt-guard running alongside (shown as a side annotation)

   **Diagram style:**
   - Use Mermaid `flowchart TD` (top-down) for overall structure
   - Use subgraphs for the 4 main paths
   - Use diamond shapes for decision points
   - Use stadium shapes (rounded) for start/end nodes
   - Keep node labels in Chinese (matching the rest of ARCHITECTURE.md)
   - Use `:::parallel` class styling for parallel execution nodes

3. After the Mermaid diagram, add a brief "流程要点" (Key Points) section as a compact table or bullet list summarizing:
   - The 4 entry paths and when to use each
   - Where parallelization happens (2 locations)
   - The 3 key gates/decision points
   - Error recovery strategy (retry once, then pause)

Formatting rules:
- Match the existing document style: Chinese headings, code blocks for diagrams, tables for structured data
- Use `---` horizontal rule after the section to match existing pattern
- Keep the Mermaid diagram readable: no more than ~40-50 nodes, use subgraphs to organize
  </action>
  <verify>
    <automated>grep -c "整体执行流程" ARCHITECTURE.md && grep -c "mermaid" ARCHITECTURE.md && grep -c "flowchart" ARCHITECTURE.md</automated>
  </verify>
  <done>
    - ARCHITECTURE.md contains a new "整体执行流程" section between 概述 and 系统分层
    - The section includes a Mermaid flowchart showing all 4 execution paths
    - Decision points (context budget, plan quality gate, verification gate) are visible as diamond nodes
    - Parallelization points are marked (4x researcher, per-wave executor)
    - A "流程要点" summary follows the diagram
    - The rest of ARCHITECTURE.md is unchanged
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected — this is a documentation-only change.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | accept | Documentation-only change, no code execution paths affected |
</threat_model>

<verification>
1. `grep "整体执行流程" ARCHITECTURE.md` returns a match
2. The Mermaid code block is valid (contains `flowchart TD` and proper node syntax)
3. All 4 paths are represented: new-project, quick, phase lifecycle, autonomous
4. The section is positioned between 概述 and 系统分层
5. No other sections of ARCHITECTURE.md were modified
</verification>

<success_criteria>
- ARCHITECTURE.md has a new visual execution flow section that a reader can scan in 30 seconds to understand the overall system flow
- The diagram accurately reflects the actual workflow definitions (autonomous.md, execute-phase.md, new-project.md, quick.md, do.md)
- Existing content is untouched
</success_criteria>

<output>
After completion, create `.planning/quick/260413-k6w-architecture-md/260413-k6w-SUMMARY.md`
</output>
