---
phase: 04-session-management
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/wf/next.md
  - wf/workflows/next.md
autonomous: true
requirements: [SESS-03]
must_haves:
  truths:
    - "/wf-next auto-detects project state and routes to the correct next workflow step"
    - "/wf-next checks the full lifecycle chain: no context -> discuss, no plan -> plan, no summary -> execute, no verification -> verify"
    - "/wf-next picks the lowest-numbered incomplete phase when multiple are incomplete"
    - "/wf-next is a thin wrapper that calls existing Skill() commands, not a reimplementation"
  artifacts:
    - path: "commands/wf/next.md"
      provides: "/wf-next command definition"
      contains: "wf:next"
    - path: "wf/workflows/next.md"
      provides: "Auto-advance workflow with state detection and routing"
      contains: "Skill("
  key_links:
    - from: "commands/wf/next.md"
      to: "wf/workflows/next.md"
      via: "execution_context reference"
      pattern: "next\\.md"
    - from: "wf/workflows/next.md"
      to: "wf/bin/wf-tools.cjs"
      via: "CLI calls for roadmap analyze and init phase-op"
      pattern: "wf-tools"
---

<objective>
Create the /wf-next command that auto-detects project state and advances to the next logical workflow step.

Purpose: SESS-03 requires a single command that eliminates the need for users to remember which workflow step comes next. It inspects the roadmap and phase artifacts, determines the next action, and routes directly to the appropriate Skill() call.

Output: One command file and one workflow file.
</objective>

<execution_context>
@.planning/phases/04-session-management/04-CONTEXT.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@wf/bin/wf-tools.cjs
@wf/bin/lib/roadmap.cjs
@wf/bin/lib/init.cjs
@wf/bin/lib/phase.cjs
@commands/wf/progress.md
@commands/wf/execute-phase.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From wf/bin/lib/roadmap.cjs:
```javascript
module.exports = { roadmapAnalyze, run };
// roadmapAnalyze(cwd) -> {
//   total_phases: number,
//   phases: [{ num, name, status, hasContext, hasPlans, hasSummary, hasVerification }],
//   current_phase: number|null,  // first non-verified phase
//   completed_phases: number
// }
// status values: 'pending' | 'discussed' | 'planned' | 'executed' | 'verified'
```

From wf/bin/lib/init.cjs:
```javascript
module.exports = { run, initPhaseOp, initNewProject, initQuick };
// initPhaseOp(cwd, phaseNumStr) -> {
//   phase_found, phase_dir, phase_number, phase_name, padded_phase,
//   has_context, has_research, has_plans, plan_count, has_verification,
//   roadmap_exists, planning_exists, project_root
// }
```

Existing command file pattern (from commands/wf/progress.md):
```markdown
---
name: wf:progress
description: ...
allowed-tools: [Read, Bash, Glob]
---
<objective>...</objective>
<execution_context>@workflow_path</execution_context>
<process>...</process>
```

Existing Skill() invocation pattern (from workflows):
```
Skill(discuss-phase, { phase: N })
Skill(plan-phase, { phase: N })
Skill(execute-phase, { phase: N })
Skill(verify-work)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /wf-next command file and auto-advance workflow</name>
  <files>commands/wf/next.md, wf/workflows/next.md</files>
  <read_first>
    - commands/wf/progress.md (command file format reference)
    - commands/wf/execute-phase.md (command with workflow reference and Skill() usage)
    - wf/workflows/execute-phase.md (workflow file with Skill() invocation patterns)
    - wf/workflows/progress.md (workflow that reads roadmap state, similar detection logic)
    - wf/bin/lib/roadmap.cjs (roadmapAnalyze output format with status values)
    - wf/bin/lib/init.cjs (initPhaseOp output format with has_context/has_plans/etc)
    - .planning/phases/04-session-management/04-CONTEXT.md (D-09 through D-12)
  </read_first>
  <action>
**File 1: `commands/wf/next.md`**

```markdown
---
name: wf:next
description: 自动检测项目状态并推进到下一逻辑步骤
allowed-tools:
  - Read
  - Bash
  - Glob
  - Agent
  - Task
---
<objective>
检测项目当前状态（哪个阶段、哪个步骤），自动路由到下一个需要执行的工作流命令。
</objective>

<execution_context>
@$HOME/.claude/wf/workflows/next.md
@$HOME/.claude/wf/references/ui-brand.md
</execution_context>

<process>
按照 @$HOME/.claude/wf/workflows/next.md 端到端执行。
</process>
```

Note: `allowed-tools` includes Agent and Task because /wf-next routes to Skill() calls that may spawn agents.

**File 2: `wf/workflows/next.md`**

Create the auto-advance workflow. This is a thin wrapper (per D-10) that detects state then calls existing workflows.

```markdown
# Auto-Advance Workflow

自动检测项目状态并推进到下一步。/wf-next 的核心价值是"自动检测"（per D-12），用户明确知道目标时应直接调用具体命令。

## 执行流程

### Step 1: 检查项目是否存在

读取 .planning/ 目录是否存在。如果不存在：

> 没有检测到项目。运行 `/wf-new-project` 初始化。

终止流程。

### Step 2: 检查是否有未恢复的暂停检查点

检查 `.planning/HANDOFF.json` 是否存在：

```bash
node "$HOME/.claude/wf/bin/wf-tools.cjs" session status
```

如果 `has_handoff` 为 true：

> 检测到暂停检查点，建议先运行 `/wf-resume` 恢复中断的工作。

显示检查点摘要后终止。优先恢复而非推进新步骤。

### Step 3: 分析路线图确定当前阶段

运行路线图分析：

```bash
node "$HOME/.claude/wf/bin/wf-tools.cjs" roadmap analyze
```

从结果中提取 `current_phase`（第一个非 verified 的阶段，per D-11）。

如果所有阶段都是 verified（`current_phase` 为 null）：

> 所有阶段已完成！里程碑可以收尾。运行 `/wf-progress` 查看详情。

终止流程。

### Step 4: 检测当前阶段的步骤状态（per D-09）

使用 init phase-op 获取阶段详情：

```bash
node "$HOME/.claude/wf/bin/wf-tools.cjs" init phase-op <current_phase>
```

根据返回字段判断下一步骤（per D-09 完整生命周期链）：

| 条件 | 下一步 | 路由命令 |
|------|--------|----------|
| `has_context` = false | discuss | `/wf-discuss-phase <N>` |
| `has_plans` = false | plan | `/wf-plan-phase <N>` |
| 存在 PLAN 但无 SUMMARY | execute | `/wf-execute-phase <N>` |
| 存在 SUMMARY 但 `has_verification` = false | verify | `/wf-verify-work` |
| `has_verification` = true 但未通过 | verify (重新) | `/wf-verify-work` |

判断"存在 SUMMARY"的方法：读取阶段目录文件列表，检查是否有包含 "SUMMARY" 的文件名。

判断"验证未通过"：读取 VERIFICATION 文件内容，检查是否包含 "FAIL"。

### Step 5: 显示检测结果并自动执行（per D-10）

向用户显示检测到的状态：

> **自动推进:** 阶段 {N} ({phase_name})
> **检测步骤:** {next_step}
> **执行命令:** {route_command}

然后直接调用对应的 Skill()（per D-10 薄包装，复用现有工作流）：

- discuss → `Skill(discuss-phase, { phase: N })`
- plan → `Skill(plan-phase, { phase: N })`
- execute → `Skill(execute-phase, { phase: N })`
- verify → `Skill(verify-work)`

不等待用户确认，直接执行（per D-12 不支持 flag 覆盖检测）。

## 边界情况

- **无 ROADMAP.md**: 提示 "没有路线图，运行 `/wf-new-project` 创建项目。"
- **ROADMAP.md 存在但无阶段**: 提示 "路线图为空，运行 `/wf-new-project` 定义需求和阶段。"
- **多个未完成阶段**: 选择编号最小的（per D-11），roadmap analyze 的 `current_phase` 已实现此逻辑。
```

The workflow must NOT implement flag override (per D-12). /wf-next always auto-detects. Users who know their target use direct commands like `/wf-execute-phase 4`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); let pass=true; if(!fs.existsSync('commands/wf/next.md')){console.log('FAIL: next.md command missing');pass=false} if(!fs.existsSync('wf/workflows/next.md')){console.log('FAIL: next.md workflow missing');pass=false} const cmd=fs.readFileSync('commands/wf/next.md','utf8'); if(!cmd.includes('wf:next')){console.log('FAIL: wrong name');pass=false} const wf=fs.readFileSync('wf/workflows/next.md','utf8'); if(!wf.includes('roadmap analyze')){console.log('FAIL: no roadmap analyze');pass=false} if(!wf.includes('init phase-op')){console.log('FAIL: no init phase-op');pass=false} if(!wf.includes('Skill(')){console.log('FAIL: no Skill() calls');pass=false} if(!wf.includes('discuss') || !wf.includes('plan') || !wf.includes('execute') || !wf.includes('verify')){console.log('FAIL: missing lifecycle steps');pass=false} if(pass) console.log('PASS: all checks passed');"</automated>
  </verify>
  <acceptance_criteria>
    - File `commands/wf/next.md` exists with frontmatter `name: wf:next`
    - commands/wf/next.md has `allowed-tools` including Agent and Task
    - commands/wf/next.md references `wf/workflows/next.md` in execution_context
    - File `wf/workflows/next.md` exists
    - Workflow contains `roadmap analyze` CLI call for phase detection
    - Workflow contains `init phase-op` CLI call for step detection
    - Workflow contains all 4 lifecycle steps: discuss, plan, execute, verify (per D-09)
    - Workflow contains `Skill(` calls for routing to existing workflows (per D-10)
    - Workflow contains `current_phase` reference for lowest-numbered selection (per D-11)
    - Workflow does NOT contain `--phase` or `--force` flag handling (per D-12)
    - Workflow checks for HANDOFF.json existence before advancing
    - Workflow handles edge cases: no project, no roadmap, all phases complete
  </acceptance_criteria>
  <done>/wf-next command and workflow created with full lifecycle detection chain (D-09), thin Skill() wrapper (D-10), lowest-phase selection (D-11), no flag override (D-12)</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI output -> workflow | Roadmap analyze and init phase-op output parsed by workflow |
| workflow -> Skill() | Auto-advance routes to agent-spawning workflows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | T (Tampering) | roadmap analyze output | accept | CLI tool is local trusted code, output is structured JSON |
| T-04-10 | E (Elevation) | Skill() auto-routing | mitigate | Route target limited to 4 known values (discuss/plan/execute/verify), no arbitrary command execution |
| T-04-11 | D (Denial of Service) | infinite advance loop | accept | /wf-next runs once per invocation, no loop. User must explicitly call again |
</threat_model>

<verification>
1. `commands/wf/next.md` exists with correct frontmatter
2. `wf/workflows/next.md` exists with all 4 lifecycle detection steps
3. Workflow uses `roadmap analyze` and `init phase-op` CLI calls
4. Workflow routes via `Skill()` to existing workflows
5. No flag override parameters in workflow (D-12)
</verification>

<success_criteria>
- /wf-next command file routable by Claude Code
- Workflow detects full lifecycle chain: context -> plan -> execute -> verify
- Workflow picks lowest-numbered incomplete phase
- Workflow is a thin wrapper over existing Skill() calls
- No flag override or manual target selection
</success_criteria>

<output>
After completion, create `.planning/phases/04-session-management/04-03-SUMMARY.md`
</output>
