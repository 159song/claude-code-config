---
phase: 02-state-safety
plan: "04"
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - wf/workflows/execute-phase.md
  - wf/workflows/autonomous.md
  - wf/workflows/new-project.md
  - commands/wf/autonomous.md
autonomous: true
requirements: [STATE-01]
must_haves:
  truths:
    - "No workflow or command file instructs agents to directly Write/Edit STATE.md for mutations"
    - "Workflow files reference wf-tools.cjs state subcommands for all state mutations"
    - "Read-only references to STATE.md are preserved (reads are safe)"
    - "Initial STATE.md creation in new-project.md is preserved (file doesn't exist yet at that point)"
  artifacts:
    - path: "wf/workflows/execute-phase.md"
      provides: "Uses CLI commands for progress updates and phase completion"
      contains: "wf-tools"
    - path: "wf/workflows/autonomous.md"
      provides: "Uses CLI commands for state updates between phases"
      contains: "wf-tools"
    - path: "wf/workflows/new-project.md"
      provides: "Adds note about using CLI for subsequent STATE.md updates"
      contains: "wf-tools"
    - path: "commands/wf/autonomous.md"
      provides: "References CLI commands instead of direct STATE.md writes"
      contains: "wf-tools"
  key_links:
    - from: "wf/workflows/execute-phase.md"
      to: "wf/bin/wf-tools.cjs state advance-plan"
      via: "Bash command in workflow instructions"
      pattern: "wf-tools.*state.*advance-plan"
    - from: "wf/workflows/autonomous.md"
      to: "wf/bin/wf-tools.cjs state begin-phase"
      via: "Bash command in workflow instructions"
      pattern: "wf-tools.*state.*begin-phase"
---

<objective>
Migrate workflow and command markdown files from direct STATE.md Write/Edit instructions to CLI command references.

Purpose: STATE-01 requires all STATE.md writes go through CLI commands. Research identified 3 workflow files and 1 command file that instruct agents to directly modify STATE.md. This plan updates those files to reference the new CLI subcommands (begin-phase, advance-plan, patch) created in Plans 01-03. Read-only references are left untouched.

Output: All workflow/command files migrated. grep audit confirms no direct STATE.md mutation instructions remain.
</objective>

<execution_context>
@/Users/zxs/.claude/get-shit-done/workflows/execute-plan.md
@/Users/zxs/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-state-safety/02-RESEARCH.md
@.planning/phases/02-state-safety/02-01-SUMMARY.md
@.planning/phases/02-state-safety/02-03-SUMMARY.md

<interfaces>
<!-- CLI commands now available after Plans 01-03 -->

Available wf-tools state subcommands:
```bash
# Read operations (already existed)
wf-tools state get <key>              # Read single key (supports dotted: progress.total_phases)
wf-tools state json                    # Full state as JSON

# Write operations (new from Plans 01-03)
wf-tools state set <key> <value>       # Write single key (supports dotted keys)
wf-tools state patch --key1 val1 --key2 val2  # Batch update multiple keys
wf-tools state merge '{"json":"obj"}'  # Deep merge JSON into frontmatter
wf-tools state validate                # Check frontmatter structure
wf-tools state begin-phase --phase <N> # Atomic phase transition (sets status, stopped_at, timestamps)
wf-tools state advance-plan --phase <N> --plan <M>  # Increment completed_plans, recalculate percent

# Validation (new module)
wf-tools validate health [--repair]    # Health check with optional auto-repair
wf-tools validate format               # Structural format check
```

Migration targets (from RESEARCH.md grep audit):
| File | Line(s) | Current Instruction | Replace With |
|------|---------|---------------------|--------------|
| wf/workflows/execute-phase.md | 107 | "更新 STATE.md 中的进度百分比" | `wf-tools state advance-plan` |
| wf/workflows/execute-phase.md | 148-150 | "当前阶段标记为 completed, 推进到下一阶段" | `wf-tools state begin-phase` |
| wf/workflows/autonomous.md | 98 | "更新 STATE.md，继续下一阶段" | `wf-tools state begin-phase` |
| wf/workflows/autonomous.md | 145 | "保存当前状态到 STATE.md" | `wf-tools state patch` |
| wf/workflows/new-project.md | 127 | "生成 .planning/STATE.md" | Keep (initial creation) + add CLI note |
| commands/wf/autonomous.md | 22 | "STATE.md -- 实时更新" | Reference CLI commands |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate workflow files to use CLI state commands</name>
  <files>wf/workflows/execute-phase.md, wf/workflows/autonomous.md, wf/workflows/new-project.md, commands/wf/autonomous.md</files>
  <read_first>
    - wf/workflows/execute-phase.md (full file -- find lines 107, 148-150, 191)
    - wf/workflows/autonomous.md (full file -- find lines 98, 145, 151)
    - wf/workflows/new-project.md (full file -- find lines 127-128, 170)
    - commands/wf/autonomous.md (full file -- find line 22)
    - .planning/phases/02-state-safety/02-RESEARCH.md ("Migration Scope" table)
  </read_first>
  <action>
**File 1: wf/workflows/execute-phase.md**

Find the section around line 107 that says:
```
3. **进度更新:** 更新 STATE.md 中的进度百分比
```
Replace with:
```
3. **进度更新:** 通过 CLI 更新进度
```bash
wf-tools state advance-plan --phase {N} --plan {M}
```
```

Find the section around lines 148-150 that says:
```
更新 STATE.md：
- 当前阶段标记为 completed
- 推进到下一阶段
```
Replace with:
```
通过 CLI 完成阶段转换：
```bash
# 标记当前阶段完成并推进到下一阶段
wf-tools state begin-phase --phase {N+1}
```

> **重要:** 禁止直接 Write/Edit STATE.md。所有状态变更必须通过 `wf-tools state` 子命令完成。
```

Find the checklist item around line 191 that says:
```
- [ ] STATE.md 正确更新
```
Replace with:
```
- [ ] STATE.md 通过 CLI 命令正确更新（`wf-tools state json` 验证）
```

**File 2: wf/workflows/autonomous.md**

Find the line around 98 that says:
```
更新 STATE.md，继续下一阶段。
```
Replace with:
```
通过 CLI 推进到下一阶段：
```bash
wf-tools state begin-phase --phase {N+1}
```
```

Find the line around 145 that says:
```
4. **Context 耗尽:** 保存当前状态到 STATE.md，提示用户在新会话中运行 `/wf-autonomous --from {current_phase}`。
```
Replace with:
```
4. **Context 耗尽:** 通过 CLI 保存当前状态，提示用户在新会话中运行 `/wf-autonomous --from {current_phase}`。
```bash
wf-tools state patch --status paused --stopped_at "Context exhausted at Phase {N}"
```
```

Find the checklist around line 151 that says:
```
- [ ] STATE.md 正确反映最终状态
```
Replace with:
```
- [ ] STATE.md 通过 CLI 命令正确更新（`wf-tools state json` 验证）
```

**File 3: wf/workflows/new-project.md**

Find the section around line 127 that says:
```
生成 `.planning/STATE.md`，记录：
```
Keep this section as-is (initial creation is acceptable when file doesn't exist yet), but add a note after the generation block:
```
> **注意:** STATE.md 初始生成后，后续所有变更必须通过 `wf-tools state` CLI 子命令完成，禁止直接 Write/Edit。
```

**File 4: commands/wf/autonomous.md**

Find the line around 22 that says:
```
- `.planning/STATE.md` — 实时更新
```
Replace with:
```
- `.planning/STATE.md` — 通过 `wf-tools state` CLI 命令更新（禁止直接 Write/Edit）
```
  </action>
  <verify>
    <automated>grep -rn "更新 STATE.md" wf/workflows/ commands/ 2>/dev/null | grep -v "CLI\|wf-tools\|禁止\|通过" | wc -l | xargs test 0 -eq</automated>
  </verify>
  <acceptance_criteria>
    - wf/workflows/execute-phase.md contains `wf-tools state advance-plan --phase`
    - wf/workflows/execute-phase.md contains `wf-tools state begin-phase --phase`
    - wf/workflows/execute-phase.md contains `禁止直接 Write/Edit STATE.md`
    - wf/workflows/autonomous.md contains `wf-tools state begin-phase --phase`
    - wf/workflows/autonomous.md contains `wf-tools state patch --status paused`
    - wf/workflows/new-project.md contains `后续所有变更必须通过` or `禁止直接 Write/Edit`
    - commands/wf/autonomous.md contains `wf-tools state` and `禁止直接 Write/Edit`
    - `grep -rn "更新 STATE.md" wf/workflows/ commands/` returns no lines that instruct direct Write/Edit (all remaining references mention CLI commands or are read-only)
    - No changes to read-only references (discuss-phase.md, progress.md lines that read STATE.md are untouched)
  </acceptance_criteria>
  <done>All workflow and command files migrated to reference CLI state commands. No file instructs direct Write/Edit of STATE.md. Read-only references preserved. Initial creation in new-project.md preserved with CLI-only note.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow markdown -> Agent behavior | Agent instructions now route through CLI instead of direct file access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-11 | Tampering | Workflow instructions referencing CLI | accept | Workflow markdown is authored by the project maintainer. The migration reduces attack surface by funneling all state mutations through validated CLI commands instead of arbitrary Write/Edit operations. Net security improvement. |
| T-02-12 | Repudiation | State changes via CLI have no audit log | accept | Git commit history provides audit trail. Each state change during execution results in a commit. Future enhancement could add explicit logging but is out of scope for Phase 2. |
</threat_model>

<verification>
```bash
# Grep audit: no direct STATE.md mutation instructions remain
grep -rn "更新 STATE.md\|写入 STATE.md\|修改 STATE.md" wf/workflows/ commands/ agents/

# All remaining STATE.md references should be read-only or CLI-routed
grep -rn "STATE.md" wf/workflows/ commands/ agents/ | grep -v "读取\|read\|检查\|查看\|wf-tools\|CLI\|禁止\|通过\|初始生成\|\.planning/STATE\.md —"

# Full test suite still passes (no code changes, only markdown)
node --test wf/bin/lib/*.test.cjs
```
</verification>

<success_criteria>
- Zero workflow/command files instruct direct Write/Edit of STATE.md for mutations
- All state mutation instructions reference `wf-tools state <subcommand>` CLI commands
- Read-only STATE.md references in discuss-phase.md, progress.md, hooks are untouched
- Initial creation in new-project.md preserved with CLI-only note for subsequent updates
- Full test suite passes (markdown-only changes, no regression possible)
</success_criteria>

<output>
After completion, create `.planning/phases/02-state-safety/02-04-SUMMARY.md`
</output>
