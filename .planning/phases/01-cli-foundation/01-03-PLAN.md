---
phase: 01-cli-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.json
  - commands/wf/do.md
  - commands/wf/new-project.md
  - commands/wf/discuss-phase.md
  - commands/wf/plan-phase.md
  - commands/wf/execute-phase.md
  - commands/wf/verify-work.md
  - commands/wf/autonomous.md
  - commands/wf/quick.md
  - commands/wf/progress.md
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02

must_haves:
  truths:
    - "No `{{WF_ROOT}}` placeholder remains in any command file"
    - "All 9 command files reference paths starting with $HOME/.claude/wf/"
    - "settings.json hook paths use $HOME-based absolute paths"
    - "All 4 hooks can be located by their settings.json paths when installed to ~/.claude/"
  artifacts:
    - path: "settings.json"
      provides: "Hook path bindings with absolute $HOME paths"
      contains: "$HOME/.claude/hooks/"
    - path: "commands/wf/do.md"
      provides: "Intent router command with resolved paths"
      contains: "$HOME/.claude/wf/"
    - path: "commands/wf/new-project.md"
      provides: "Project init command with resolved paths"
      contains: "$HOME/.claude/wf/"
    - path: "commands/wf/plan-phase.md"
      provides: "Plan phase command with resolved paths"
      contains: "$HOME/.claude/wf/"
  key_links:
    - from: "settings.json"
      to: "hooks/wf-context-monitor.js"
      via: "PostToolUse hook command path"
      pattern: "\\$HOME/\\.claude/hooks/wf-context-monitor\\.js"
    - from: "settings.json"
      to: "hooks/wf-session-state.sh"
      via: "SessionStart hook command path"
      pattern: "\\$HOME/\\.claude/hooks/wf-session-state\\.sh"
    - from: "commands/wf/do.md"
      to: "wf/workflows/do.md"
      via: "@$HOME/.claude/wf/workflows/do.md reference"
      pattern: "@\\$HOME/\\.claude/wf/workflows/do\\.md"
---

<objective>
Fix hook paths in settings.json and replace all {{WF_ROOT}} placeholders in command files.

Purpose: All 4 hooks are currently silently non-functional because settings.json points to `.claude/hooks/` but files live at `hooks/`. All 9 command files are broken because `{{WF_ROOT}}` is never resolved. This plan fixes both critical path issues so the WF system actually works when installed.

Output: settings.json with correct $HOME-based hook paths, 9 command files with all 31 `{{WF_ROOT}}` occurrences replaced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cli-foundation/01-CONTEXT.md
@.planning/phases/01-cli-foundation/01-RESEARCH.md

<interfaces>
<!-- Current broken paths that need fixing -->

settings.json current hook paths (all broken -- RESEARCH Pitfall 1):
```json
"command": "bash .claude/hooks/wf-session-state.sh"       // should be: bash "$HOME/.claude/hooks/wf-session-state.sh"
"command": "node .claude/hooks/wf-context-monitor.js"      // should be: node "$HOME/.claude/hooks/wf-context-monitor.js"
"command": "node .claude/hooks/wf-prompt-guard.js"         // should be: node "$HOME/.claude/hooks/wf-prompt-guard.js"
"command": "node .claude/hooks/wf-statusline.js"           // should be: node "$HOME/.claude/hooks/wf-statusline.js"
```

Command file WF_ROOT pattern (31 occurrences across 9 files -- RESEARCH Appendix):
```markdown
@{{WF_ROOT}}/wf/workflows/do.md        →  @$HOME/.claude/wf/workflows/do.md
@{{WF_ROOT}}/wf/references/ui-brand.md →  @$HOME/.claude/wf/references/ui-brand.md
```

Full inventory from RESEARCH:
- do.md: 3 occurrences (workflows/do.md, references/ui-brand.md)
- new-project.md: 5 occurrences
- discuss-phase.md: 3 occurrences
- plan-phase.md: 4 occurrences
- execute-phase.md: 4 occurrences
- verify-work.md: 3 occurrences
- autonomous.md: 3 occurrences
- quick.md: 3 occurrences
- progress.md: 3 occurrences
Total: 31 occurrences
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix hook paths in settings.json</name>
  <files>settings.json</files>
  <read_first>
    - settings.json (current broken hook paths -- must see exact structure before editing)
    - .planning/phases/01-cli-foundation/01-RESEARCH.md (Pitfall 1: Hook Path Mismatch, and Code Example: Hook Path Fix)
  </read_first>
  <action>
Update `settings.json` to fix all 4 hook paths from relative `.claude/hooks/` to absolute `$HOME/.claude/hooks/` with proper quoting (per D-04, D-05, fixes Pitfall 1).

Exact replacements:

1. SessionStart hook:
   - FROM: `"command": "bash .claude/hooks/wf-session-state.sh"`
   - TO: `"command": "bash \"$HOME/.claude/hooks/wf-session-state.sh\""`

2. PostToolUse hook (context monitor):
   - FROM: `"command": "node .claude/hooks/wf-context-monitor.js"`
   - TO: `"command": "node \"$HOME/.claude/hooks/wf-context-monitor.js\""`

3. PreToolUse hook (prompt guard):
   - FROM: `"command": "node .claude/hooks/wf-prompt-guard.js"`
   - TO: `"command": "node \"$HOME/.claude/hooks/wf-prompt-guard.js\""`

4. StatusLine:
   - FROM: `"command": "node .claude/hooks/wf-statusline.js"`
   - TO: `"command": "node \"$HOME/.claude/hooks/wf-statusline.js\""`

Preserve the exact JSON structure (2-space indentation, existing matcher values, timeout values). Only modify the `"command"` string values. Keep timeout:10 for PostToolUse and timeout:5 for PreToolUse.
  </action>
  <verify>
    <automated>grep -c '\\$HOME/.claude/hooks/' settings.json | xargs -I{} test {} -eq 4 && echo "OK: 4 $HOME paths" || echo "FAIL: expected 4 $HOME hook paths" && grep -c '{{WF_ROOT}}\|\.claude/hooks/wf-' settings.json | xargs -I{} test {} -eq 0 && echo "OK: no old paths" || echo "FAIL: old paths remain"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '\\$HOME/.claude/hooks/' settings.json` outputs exactly 4
    - `grep -c '.claude/hooks/wf-' settings.json` outputs exactly 0 (no old relative paths remain, only $HOME-prefixed ones exist but they contain `.claude/hooks/wf-` as a substring of `$HOME/.claude/hooks/wf-` so check for the old pattern without $HOME prefix)
    - Actually, more precisely: `grep '"command"' settings.json | grep -cv '\\$HOME'` outputs 0 (all command values contain $HOME)
    - `grep 'timeout' settings.json | wc -l` >= 2 (timeouts preserved)
    - `node -e "const s=require('./settings.json'); console.log(s.hooks.SessionStart[0].hooks[0].command)"` outputs string containing `$HOME/.claude/hooks/wf-session-state.sh`
    - `node -e "const s=require('./settings.json'); console.log(s.statusLine.command)"` outputs string containing `$HOME/.claude/hooks/wf-statusline.js`
    - `node -e "JSON.parse(require('fs').readFileSync('settings.json','utf8')); console.log('valid JSON')"` outputs "valid JSON"
  </acceptance_criteria>
  <done>All 4 hook paths in settings.json use $HOME-based absolute paths with double-quote escaping. JSON is valid. Timeout values preserved. No relative .claude/hooks/ paths remain.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all {{WF_ROOT}} in 9 command files</name>
  <files>commands/wf/do.md, commands/wf/new-project.md, commands/wf/discuss-phase.md, commands/wf/plan-phase.md, commands/wf/execute-phase.md, commands/wf/verify-work.md, commands/wf/autonomous.md, commands/wf/quick.md, commands/wf/progress.md</files>
  <read_first>
    - commands/wf/do.md (example of {{WF_ROOT}} pattern)
    - commands/wf/new-project.md (has 5 occurrences, most complex)
    - commands/wf/plan-phase.md (has 4 occurrences)
    - commands/wf/execute-phase.md (has 4 occurrences)
    - .planning/phases/01-cli-foundation/01-RESEARCH.md (Appendix: WF_ROOT Replacement Inventory -- lists all 31 occurrences, Pitfall 4 about $HOME expansion)
  </read_first>
  <action>
Replace all `{{WF_ROOT}}` occurrences with `$HOME/.claude` in all 9 command files under `commands/wf/` (per D-04, D-06).

The replacement pattern is:
- `@{{WF_ROOT}}/wf/workflows/do.md` becomes `@$HOME/.claude/wf/workflows/do.md`
- `@{{WF_ROOT}}/wf/references/ui-brand.md` becomes `@$HOME/.claude/wf/references/ui-brand.md`
- Any other `{{WF_ROOT}}` reference becomes `$HOME/.claude` (note: the `/wf/...` part after WF_ROOT is kept as-is)

This is a simple global find-and-replace: `{{WF_ROOT}}` → `$HOME/.claude`

Process each file:
1. Read the file
2. Replace every occurrence of `{{WF_ROOT}}` with `$HOME/.claude`
3. Verify the replacement count matches the RESEARCH inventory:
   - do.md: 3
   - new-project.md: 5
   - discuss-phase.md: 3
   - plan-phase.md: 4
   - execute-phase.md: 4
   - verify-work.md: 3
   - autonomous.md: 3
   - quick.md: 3
   - progress.md: 3
   - Total: 31
4. Write the updated file
5. Verify no `{{WF_ROOT}}` remains in any file

**Important per Pitfall 4:** If Claude Code's `@` reference does not expand `$HOME`, the implementation task may need to use the literal expanded path. However, the research notes GSD uses `$HOME` in settings.json successfully. Start with `$HOME/.claude` per D-04. If verification reveals Claude Code cannot resolve `$HOME` in `@` references, escalate -- do NOT silently change to hardcoded `/Users/zxs/.claude`.
  </action>
  <verify>
    <automated>grep -r '{{WF_ROOT}}' commands/wf/ | wc -l | xargs -I{} test {} -eq 0 && echo "OK: no WF_ROOT remaining" || echo "FAIL: WF_ROOT still present" && grep -rc '\$HOME/.claude/wf/' commands/wf/ | paste -sd+ - | bc | xargs -I{} test {} -ge 31 && echo "OK: >= 31 $HOME paths" || echo "FAIL: fewer than 31 replacements"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r '{{WF_ROOT}}' commands/wf/ | wc -l` outputs 0 (zero remaining placeholders)
    - `grep -c '\$HOME/.claude' commands/wf/do.md` outputs at least 3
    - `grep -c '\$HOME/.claude' commands/wf/new-project.md` outputs at least 5
    - `grep -c '\$HOME/.claude' commands/wf/discuss-phase.md` outputs at least 3
    - `grep -c '\$HOME/.claude' commands/wf/plan-phase.md` outputs at least 4
    - `grep -c '\$HOME/.claude' commands/wf/execute-phase.md` outputs at least 4
    - `grep -c '\$HOME/.claude' commands/wf/verify-work.md` outputs at least 3
    - `grep -c '\$HOME/.claude' commands/wf/autonomous.md` outputs at least 3
    - `grep -c '\$HOME/.claude' commands/wf/quick.md` outputs at least 3
    - `grep -c '\$HOME/.claude' commands/wf/progress.md` outputs at least 3
    - Each command file's YAML frontmatter (---...---) is still valid (name, description fields intact)
    - `head -3 commands/wf/do.md` still shows `---` on line 1 (frontmatter intact)
  </acceptance_criteria>
  <done>All 31 {{WF_ROOT}} occurrences across 9 command files replaced with $HOME/.claude. Zero placeholders remain. All command file frontmatter intact. All @-references now point to $HOME/.claude/wf/ paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| settings.json → shell execution | Hook command strings are executed by Claude Code's shell runner |
| $HOME expansion → file access | Path resolution depends on shell's $HOME variable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Hook command injection via settings.json | accept | settings.json is committed to git and maintained by the project owner. No untrusted input enters hook commands. Users review settings.json changes in diffs. |
| T-03-02 | Spoofing | $HOME environment variable override | accept | Running as local user; $HOME is set by the OS login session. Not a realistic attack vector for a local CLI tool. |
| T-03-03 | Information Disclosure | Absolute paths in committed files | accept | $HOME is not expanded in committed files (stays as `$HOME`). Only the shell expands it at runtime. No actual user paths leak into git. |
</threat_model>

<verification>
1. `grep -r '{{WF_ROOT}}' commands/wf/ settings.json | wc -l` = 0 (no placeholders anywhere)
2. `grep -c '\\$HOME/.claude/' settings.json` >= 4 (all hooks use $HOME)
3. `grep -rc '\\$HOME/.claude/' commands/wf/ | awk -F: '{s+=$2}END{print s}'` >= 31 (all replacements done)
4. settings.json is valid JSON: `node -e "JSON.parse(require('fs').readFileSync('settings.json','utf8'))"`
5. Each command file retains its YAML frontmatter structure
</verification>

<success_criteria>
- Zero `{{WF_ROOT}}` placeholders remain in any file (INFRA-02 complete)
- All 4 hook paths in settings.json use $HOME-based absolute paths (INFRA-01 complete)
- settings.json is valid JSON with timeout values preserved
- All 9 command files have correct @$HOME/.claude/wf/ references
- Command file frontmatter (name, description, allowed-tools) unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-cli-foundation/01-03-SUMMARY.md`
</output>
