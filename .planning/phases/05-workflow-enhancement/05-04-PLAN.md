---
phase: 05-workflow-enhancement
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/wf-prompt-guard.js
  - hooks/wf-prompt-guard.test.cjs
  - wf/references/anti-patterns.md
  - wf/references/context-budget.md
  - wf/references/continuation-format.md
autonomous: true
requirements:
  - WF-04
  - WF-05
  - WF-06
must_haves:
  truths:
    - "Prompt guard uses negative lookahead to reduce false positives on legitimate workflow content"
    - "File suffix whitelist downgrades .md matches to informational severity"
    - "Prompt guard remains advisory-only and always exits 0"
    - "Three new reference docs exist with 50-150 lines of substantive content each"
    - "WF-06 is confirmed already implemented with no new work needed"
  artifacts:
    - path: "hooks/wf-prompt-guard.js"
      provides: "Enhanced prompt guard with negative lookahead and whitelist"
      contains: "REDUCED_SEVERITY_EXTENSIONS"
    - path: "hooks/wf-prompt-guard.test.cjs"
      provides: "False-positive regression tests"
      contains: "test("
    - path: "wf/references/anti-patterns.md"
      provides: "Workflow anti-patterns reference"
      min_lines: 50
    - path: "wf/references/context-budget.md"
      provides: "Context budget management reference"
      min_lines: 50
    - path: "wf/references/continuation-format.md"
      provides: "Session continuation format spec"
      min_lines: 50
  key_links:
    - from: "hooks/wf-prompt-guard.js"
      to: "settings.json"
      via: "PreToolUse hook binding"
      pattern: "wf-prompt-guard"
---

<objective>
Harden the prompt guard with negative lookahead patterns and create three reference documents for the WF system.

Purpose: The prompt guard currently has 13 raw regex patterns that trigger false positives on legitimate content (e.g., docs about prompt injection, workflow references). Negative lookahead plus file whitelist (D-10) reduces noise while maintaining security. The three reference docs (D-12) complete the documentation suite. WF-06 is confirmed done (D-14).

Output: Enhanced `wf-prompt-guard.js`, new test file, and three new reference docs in `wf/references/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-workflow-enhancement/05-CONTEXT.md
@.planning/phases/05-workflow-enhancement/05-RESEARCH.md

<interfaces>
From hooks/wf-prompt-guard.js (current 13 patterns):
- High-confidence: ignore previous/above instructions, disregard previous, forget instructions, override system prompt
- Context-sensitive (false-positive prone): "you are now a/an/the", "act as a/an/the", "pretend you are", "from now on you"
- System markers: print/reveal system prompt, XML system/assistant/human tags, [SYSTEM], [INST]
- Also checks invisible Unicode characters

Current hook flow: stdin JSON -> check Write|Edit on .planning/ -> scan patterns -> advisory output -> exit 0

From settings.json PreToolUse hook: 5-second timeout for wf-prompt-guard.js

From wf/references/gates.md and verification-patterns.md: Chinese language, 80-90 lines, H1/H2/H3 structure, tables, pragmatic style
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add negative lookahead and file whitelist to prompt guard with tests</name>
  <files>hooks/wf-prompt-guard.js, hooks/wf-prompt-guard.test.cjs</files>
  <read_first>
    hooks/wf-prompt-guard.js
    settings.json
  </read_first>
  <behavior>
    - Test: content "act as a plan" does NOT trigger (excluded by existing negative lookahead for workflow terms)
    - Test: content "act as a hacker" DOES trigger
    - Test: content matching "you are now a malicious agent" DOES trigger
    - Test: .md file path produces reduced severity output (info message, not warning)
    - Test: .js file path produces full severity output (warning message)
    - Test: invisible Unicode characters still trigger regardless of file type
    - Test: hook always exits with code 0 (advisory mode preserved per D-11)
    - Test: high-confidence patterns (disregard previous, override system prompt) still trigger
  </behavior>
  <action>
**Enhance `hooks/wf-prompt-guard.js`:**

1. Add `REDUCED_SEVERITY_EXTENSIONS` constant after INJECTION_PATTERNS:
   ```
   const REDUCED_SEVERITY_EXTENSIONS = ['.md', '.txt', '.log'];
   ```

2. Update the context-sensitive patterns in INJECTION_PATTERNS with negative lookahead. Keep the 5 high-confidence patterns unchanged (they rarely false-positive). For the 4 context-sensitive patterns:

   Pattern "you are now a/an/the": Add negative lookahead excluding WF agent role names: planner, executor, verifier, researcher, roadmapper, agent, workflow, step. The lookahead goes after the `(?:a|an|the)\s+` part.

   Pattern "act as a/an/the": Extend the existing exclusion list `(?!plan|phase|wave)` to also include: step, task, agent, researcher, executor, planner, verifier, roadmapper. Use word boundaries `\b` on each term.

   Pattern "pretend you are/to be": Add negative lookahead excluding implementation verbs: implementing, building, creating, testing, executing, planning, verifying.

   Pattern "from now on you are/will/should/must": Add negative lookahead excluding directive verbs used in workflows: follow, execute, implement, verify, check, create, build, test.

   Keep system marker patterns (XML tags, [SYSTEM], [INST]) and system prompt extraction pattern unchanged.

3. Add severity downgrade logic after the findings loop, before the output block:
   - Extract file extension with `path.extname(filePath).toLowerCase()`
   - Check if extension is in REDUCED_SEVERITY_EXTENSIONS
   - If reduced severity: use informational message "[Info] 文档文件 {basename} 触发了 {count} 个模式检测（文档文件降级为信息提示）。"
   - If full severity: keep existing warning message unchanged

4. Verify no `process.exit(1)` exists anywhere in the file. The hook must always exit 0.

**Create `hooks/wf-prompt-guard.test.cjs`:**

Use Node.js built-in test runner (`node:test` and `node:assert`). Each test spawns the hook as a child process using `child_process.execFileSync('node', [hookPath])` with JSON piped via stdin using `{ input: JSON.stringify(testData) }`.

Test data format for each test:
```javascript
const testInput = {
  tool_name: 'Write',
  tool_input: {
    file_path: '.planning/phases/test/TEST.md',
    content: 'the test content here'
  }
};
```

Write 8 tests covering the behavior cases listed above. For each test, assert:
- Exit code is always 0
- stdout is empty (no findings) OR contains expected advisory text
- For reduced severity tests, stdout contains "[Info]" instead of "PROMPT INJECTION"
  </action>
  <verify>
    <automated>node hooks/wf-prompt-guard.test.cjs</automated>
  </verify>
  <acceptance_criteria>
    - wf-prompt-guard.js contains "REDUCED_SEVERITY_EXTENSIONS" constant with ".md" in array
    - wf-prompt-guard.js contains negative lookahead with "planner" or "executor" in at least 2 patterns
    - wf-prompt-guard.js contains severity logic checking file extension via path.extname
    - wf-prompt-guard.js does NOT contain process.exit(1)
    - wf-prompt-guard.test.cjs exists with at least 6 test blocks
    - node hooks/wf-prompt-guard.test.cjs exits with code 0
  </acceptance_criteria>
  <done>Prompt guard has negative lookahead reducing false positives, file whitelist for severity downgrade, and regression tests</done>
</task>

<task type="auto">
  <name>Task 2: Create three reference documents</name>
  <files>wf/references/anti-patterns.md, wf/references/context-budget.md, wf/references/continuation-format.md</files>
  <read_first>
    wf/references/gates.md
    wf/references/verification-patterns.md
    wf/references/agent-contracts.md
    hooks/wf-context-monitor.js
    wf/bin/lib/session.cjs
  </read_first>
  <action>
Create 3 new reference documents in `wf/references/`. Each 50-150 lines (per D-13), written in Chinese, following the pragmatic style of `gates.md` (81 lines) and `verification-patterns.md` (89 lines).

**1. `wf/references/anti-patterns.md` (~80-100 lines):**

Title: `# 工作流反模式`

Sections:
- **概述**: What anti-patterns are and why avoiding them matters (2-3 lines)
- **执行反模式** (4-5 entries): Direct STATE.md writes (use CLI), Deep Task() nesting (use Skill() chains), Skipping verification, Ignoring context budget warnings
- **规划反模式** (3-4 entries): Oversized plans (>3 tasks), Missing dependency analysis, Vague task descriptions
- **Agent 反模式** (3 entries): Agent without completion marker, Agent ignoring context budget, Agent making architectural decisions
- **恢复反模式** (3 entries): Infinite retry loops (max 1 gap closure), Skipping failed phases (per D-02), Amending commits instead of new ones

Each entry: pattern name, what happens, what to do instead (1-2 lines each).

**2. `wf/references/context-budget.md` (~80-100 lines):**

Title: `# 上下文预算管理`

Sections:
- **概述**: Context window is finite; budget management prevents quality degradation
- **预算阈值** table: >50% normal, 35-50% watch, 25-35% WARNING (hook injects), <25% CRITICAL, <15% emergency
- **自治模式预算**: Each phase cycle consumes ~30-50% context; check remaining > 40% before starting new phase; if low, pause and save state
- **每任务预算** table: file count vs context impact, complexity vs context cost
- **节省技巧**: Use /clear between phases, keep 2-3 tasks per plan, use CLI tools for state queries

**3. `wf/references/continuation-format.md` (~70-90 lines):**

Title: `# 会话续接格式`

Sections:
- **概述**: How WF persists state for cross-session continuation
- **HANDOFF.json 格式**: JSON schema with field descriptions table (version, timestamp, phase, plan, step, stopped_at, git_branch, context_remaining_pct)
- **.continue-here.md 格式**: Human-readable summary at project root
- **恢复流程**: /wf-resume reads HANDOFF, validates branch, routes to Skill(), cleans up
- **自治模式续接**: Autonomous saves HANDOFF on pause; new session uses --from N
- **安全约束**: step whitelist (discuss/plan/execute/verify), phase must be positive number, handoff files deleted after resume

WF-06 note: git commit --files selective staging was implemented in Phase 1/2 (per D-14). This plan covers WF-06 for requirement traceability only -- no implementation work needed.
  </action>
  <verify>
    <automated>test $(wc -l < wf/references/anti-patterns.md) -ge 50 && test $(wc -l < wf/references/context-budget.md) -ge 50 && test $(wc -l < wf/references/continuation-format.md) -ge 50 && grep -qP '[\x{4e00}-\x{9fff}]' wf/references/anti-patterns.md && grep -qP '[\x{4e00}-\x{9fff}]' wf/references/context-budget.md && grep -qP '[\x{4e00}-\x{9fff}]' wf/references/continuation-format.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - wf/references/anti-patterns.md exists with 50-150 lines (wc -l check)
    - wf/references/anti-patterns.md first line is "# 工作流反模式"
    - wf/references/anti-patterns.md mentions "Skill()" as alternative to Task nesting
    - wf/references/context-budget.md exists with 50-150 lines
    - wf/references/context-budget.md contains "35%" and "25%" threshold values
    - wf/references/context-budget.md mentions "wf-context-monitor"
    - wf/references/continuation-format.md exists with 50-150 lines
    - wf/references/continuation-format.md contains "HANDOFF.json"
    - wf/references/continuation-format.md mentions step whitelist
    - All three files contain Chinese characters (written in Chinese)
  </acceptance_criteria>
  <done>Three reference documents created: anti-patterns, context-budget, continuation-format; each 50-150 lines in Chinese pragmatic style</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File content -> regex engine | Large content could cause regex backtracking |
| Reference docs -> agent context | Docs become part of agent instructions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-11 | Denial of Service | Regex backtracking | mitigate | Keep negative lookaheads simple and anchored; avoid nested quantifiers; 5-second timeout in settings.json as safety net |
| T-05-12 | Spoofing | Lookahead bypass | accept | Prompt guard is advisory defense-in-depth (per D-11), not sole defense; lookaheads reduce noise |
| T-05-13 | Tampering | Reference doc injection | accept | Reference docs are read-only in wf/references/; prompt guard monitors .planning/ writes |
</threat_model>

<verification>
- Content matching "act as a plan" does NOT trigger prompt guard
- Content matching "disregard all previous" DOES trigger
- .md file targets produce reduced-severity messages
- All 3 reference docs exist with 50+ lines each
- node hooks/wf-prompt-guard.test.cjs passes
</verification>

<success_criteria>
- Prompt guard has negative lookahead on context-sensitive patterns (D-10)
- File suffix whitelist downgrades .md matches (D-10)
- Advisory mode preserved, always exits 0 (D-11)
- 3 reference docs in wf/references/ with 50-150 lines each (D-12, D-13)
- WF-06 confirmed done via D-14
</success_criteria>

<output>
After completion, create `.planning/phases/05-workflow-enhancement/05-04-SUMMARY.md`
</output>
